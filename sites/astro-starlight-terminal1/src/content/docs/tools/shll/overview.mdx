---
title: shll
slug: shll
description: Meta-CLI to install, update, and shell-wire the whole toolkit in one command.
---

import GithubButton from '../../../../components/GithubButton.astro';
import InstallOneLiner from '../../../../components/InstallOneLiner.astro';

<GithubButton tool="shll" />

`shll` is the meta-CLI that bootstraps the whole AI coding toolkit — one command to install, update, and shell-wire every tool at once. It composes the per-tool CLIs rather than replacing them: `shll install` brings down each binary, and `shll update` keeps them current. It's the first thing you run and the one command you keep around to keep everything in sync.

## Install

<InstallOneLiner tool="shll" />

## How it fits

`shll` is the bootstrap for everything else — see [Install everything](/getting-started/install/) to set up the toolkit in one go. From there the loop runs: **[idea](/idea/)** captures work, **[fab-kit](/fab-kit/)** plans it, **[wt](/wt/)** isolates it, and **[run-kit](/run-kit/)** watches it.

## Where to next

- **[Readme](/shll/readme/)** — the full canonical guide, synced from shll's own repo.
- **[Commands](/shll/commands/)** — every subcommand and flag.
