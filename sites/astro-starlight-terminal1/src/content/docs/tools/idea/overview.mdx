---
title: idea
description: Plain-Markdown backlog tracker — worktree-aware, queryable from the CLI, feeds /fab-new.
---

import GithubButton from '../../../../components/GithubButton.astro';

<GithubButton tool="idea" />

`idea` captures and manages a per-repo backlog in a plain Markdown file (`fab/backlog.md`) — worktree-aware, queryable from the CLI, and the same file `fab-kit`'s `/fab-new` reads.

## Where to next

- **[Readme](../readme/)** — the full canonical guide, synced from idea's own repo.
- **[Commands](../commands/)** — every subcommand and flag.
