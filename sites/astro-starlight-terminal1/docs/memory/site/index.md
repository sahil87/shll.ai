---
description: "Site-specific implementation behavior for the astro-starlight-terminal1 build"
---
# site

> Hand-maintained (this tree is outside `fab memory-index`). Descriptions come from each file's `description:` frontmatter.

| File | Description | Last Updated |
|------|-------------|-------------|
| [homepage-terminal](homepage-terminal.md) | The homepage's interactive terminal prompt (changes `9vbo`, `n23o`, `23xc`, `by18`, `cuur`, `o33t`, `cdbr`, `37ng`, `tx5p`): the progressive-enhancement boundary (static `<pre class="shell-session">` transcript is the no-JS source of truth; `TerminalPrompt.astro` client island upgrades ONLY the final `[data-terminal-prompt]` line into a focusable `contenteditable` input, no autofocus on load), the static client-side command dispatch map (help/ls/cd·open/install/version/theme/history/clear + eggs whoami·sudo·echo·man·shll·sl·fortune·exit·:q; unknown commands get a did-you-mean suggestion or the `type 'help'` tail), the shell affordances on `onKeydown` (↑/↓ command history with sessionStorage persistence + `ignoredups`, bash-like Tab-completion, Ctrl-L clear / Ctrl-C cancel — all upholding the exactly-one-trailing-prompt invariant), the `theme` → `<starlight-theme-select>` `change`-event sync, the `.terminal-window` CSS chrome (1px border + thin title bar + 3 dimmed dots — a deliberate reversal of `260517-pdsp-terminal-skin`'s no-chrome decision), the above-fold activation pass (change `23xc`: `[data-has-hero]`-scoped hero rhythm tightening, the resting-state top anchor — greeting line first visible via an inline `padding-bottom` filler; bottom-pinning resumes on the first prompt-emitting interaction — the activation-time greeting line, and the one-shot 4 s idle ghost hint `$ ▊try 'ls' ⏎`, aria-hidden, visual-only, reduced-motion-safe), the touch-support pass (change `by18`: tap-to-focus riding the synthesized-click path hardened with `touch-action: manipulation` — no touchend fallback; the `commitLine(focusNext)` extraction shared by Enter and chips; the island-injected `.terminal-cmdbar` quick-command chips ls/help/version/install, revealed only under `(hover: none) and (pointer: coarse)`, committing via `commitLine(false)` — chip taps never focus the input, so the soft keyboard never pops over output), and the command-polish + accessible-output pass (change `cuur`: the dependency-free `src/lib/terminal-suggest.ts` did-you-mean suggester — OSA Damerau-Levenshtein, adjacent transposition = 1, threshold 1 for ≤3-char inputs else 2, candidates = `Object.keys(COMMANDS)` incl. eggs — wired into the not-found line and `help <arg>`; the `HELP_DETAIL` per-command help record covering every `COMMANDS` key incl. the unlisted eggs; the shared `navigateWithBeat` 900 ms cancellable navigation beat for cd/open/install — synopsis + clickable anchor + `(Ctrl-C to cancel)`, Ctrl-C clears `pendingNav`; `aria-live="polite"`/`aria-atomic="false"` output announcements on `pre.shell-session` with each fresh prompt's `$` aria-hidden; and the `prefers-reduced-motion` CSS gate holding both blink cursors steady-visible), and the GNU-utils delight pass (change `o33t`: 33 new unlisted GNU-ish easter-egg commands — pwd·uname·id·date·uptime·env, the read-only refusals rm·mkdir·touch·mv·cp·chmod·chown, diff·tar·make, editors vim·vi·emacs·nano, network deniers curl·wget, pagers less·more, kill, silent true·false, seq·ps·sha256sum·grep, and the animated yes·tail·rm-deluxe + the `sl` crossing animation — under the user-approved curation rule (teach the toolkit / reward terminal-culture recognition / demo interactivity), bare `help` list byte-identical; the `FAKE_ENV` record single-sourcing `env` output and `echo`'s `$VAR` expansion; the dependency-free `src/lib/terminal-eggs.ts` lib (`expandVars`/`seqLines`+`SEQ_GEN_CAP`/`classifyRm`/`classifyTar`, 22 `node --test` tests) with the `Object.hasOwn` own-property guard on EVERY record lookup keyed by user input — `run()`'s COMMANDS dispatch, the `man` HELP_DETAIL bridge, `helpFor`, and `expandVars` (review-cycle lesson: unguarded reads resolved prototype-chain names like `constructor`/`__proto__` and bricked the prompt); the `printBeforePrompt`-based one-at-a-time line streamer (`startStream`/`stopStream` — Ctrl-C, Ctrl-L, and `commitLine` all kill it incl. the `streamFollowup` beat; reduced-motion prints all-at-once; deliberately unlike `pendingNav`, which `commitLine` does NOT clear); `Line.ariaHidden` live-region noise control (art/frame lines hidden, one summary/punchline announced; the existing `shll`/`sl` art retrofitted; `tail`'s slow meaningful frames the deliberate announced exception); and the rm-deluxe transcript wipe via `session.replaceChildren(liveLine)` preserving the live prompt element with conditional focus restore — skipped under reduced motion), and the full-roster cheatsheet (change `cdbr`: the `cheatsheet` command — the sanctioned full-roster reveal superseding the accidental empty-prompt Tab dump as the discoverable index (the dump itself stays as-is): opening `the full roster — you found it.`, five fixed groups navigate / look around / do things (try to) / classics / real utilities each a dim header + two-space-indented ` · `-joined lines, closing `(each one: help <command>)`; the `CHEATSHEET_GROUPS` + `CHEATSHEET_ALIASES` anti-drift contract — coverage recomputed at runtime on every run so uncovered COMMANDS keys append under `uncategorized` (never a silent omission), stale group entries dropped at render (all-stale groups omitted, no orphan header), aliases vi→vim/more→less/:q→exit folded into their primaries; the dependency-free `src/lib/terminal-cheatsheet.ts` (`buildCheatsheet`/`chunkLine`/`UNCATEGORIZED`, 14 `node --test` tests) with the 74-joined-char + 2-space-indent width budget (`CHEAT_LINE_WIDTH`/`CHEAT_SEP`); and the FIRST sanctioned help-output change — the bare-help footer tip now ends "— or type 'cheatsheet' for everything." while the curated 13-command list and the eggs-stay-unlisted stance stayed byte-identical), and the tool-name cards (change `37ng` — the funnel fix: bare tool names idea·hop·fab-kit·wt·run-kit·tu, plus the binary aliases rk·fab, print that tool's card mechanically sourced from repo-root `help/<slug>.json`: build-time frontmatter readdir + `HelpDocSchema.safeParse` with per-tool skip-degrade (VersionTable stays the build's defect gate), a slim 7-tool payload (`{short, usage, commands[].{name,short}}`, `<` escaped) in ONE `application/json` `data-terminal-help` template element the island parses guarded at activation (absent/malformed → SYNOPSIS+nav fallback); the dependency-free `src/lib/terminal-toolcard.ts` (`buildToolCard`/`findSubcommand`/`formatSubcommandLine` + the `stripUsagePrefix`/`stripToolPrefix` producer-quirk normalizations, `TOOLCARD_SUB_CAP = 8` / `TOOLCARD_NAME_COL = 12` / `TOOLCARD_LINE_WIDTH = 76` with every line incl. the header clamped, 23 `node --test` tests); `hop <sub>` prints the one padded subcommand line + a see-more anchor; the cheatsheet gains a `tools` group placed FIRST with rk/fab alias-folded (shll stays in classics); the shll splash keeps its art and gains the closing nav line; COMMANDS grows 52 → 60 with bare `help` byte-identical — a bare name informs in place, `cd` keeps navigating), and the share→replay loop (change `tx5p`: the `share` command — DOM-order span.shell-line textContent serialization with per-line trailing trim, the live prompt rendered by the lib as a bare `$`, visually-present aria-hidden art included, framed by `SHARE_HEADER` + the literal `# replayed from https://shll.ai` footer + a conditional `# replay it:` link footer; clipboard via navigator.clipboard.writeText → guarded execCommand textarea fallback with finally'd removal + GENERIC focus restore → honest in-terminal failure block; the `#play=` deep-link grammar parsed at activation from the RAW fragment (`new URL(location.href).hash`, never the engine-decoded location.hash) — split on raw `,`/`;` FIRST then per-token decodeURIComponent, a malformed token dropped alone — validated by the shared `isReplayable` predicate (`Object.hasOwn` inside the lib + `REPLAY_DENY = cd·open·install·share`, user-confirmed), survivors capped at `REPLAY_CAP = 10`, hash left in place; the replay engine typing at `REPLAY_TYPE_MS = 70` and committing via `commitLine(false)` (no focus/scroll initiation; mid-replay user-established focus conditionally restored across freezeInput) with `REPLAY_GAP_MS = 700` gaps after a `REPLAY_START_DELAY_MS = 600` start, fully synchronous under reduced motion; the kill switch — a capture-phase document-level keydown listener installed by startReplay/removed by stopReplay (any keystroke regardless of focus), plus onKeydown first statements, chip taps, and paste, while commitLine deliberately does NOT stop replay; page-view-scoped `sessionCommands` recording in commitLine behind the same predicate (never history[], never persisted), buildPlayHash capping at the first 10 in commit order; the dependency-free `src/lib/terminal-share.ts` (parsePlayHash/buildPlayHash/serializeTranscript/firstWord/isReplayable, 27 `node --test` tests — the fifth lib); `share` appended LAST in COMMANDS+HELP_DETAIL (60 → 61 keys), the cheatsheet `navigate` group, and bare `help`'s curated list — the SECOND sanctioned help-output change ever, user-confirmed; chips/greeting/hint unchanged) | 2026-06-12 |
