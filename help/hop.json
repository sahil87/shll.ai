{
  "tool": "hop",
  "version": "v0.1.11",
  "captured_at": "2026-06-04T08:36:42Z",
  "schema_version": 1,
  "root": {
    "name": "hop",
    "path": "hop",
    "short": "locate, open, and operate on repos from hop.yaml.",
    "usage": "hop",
    "text": "hop — locate, open, and operate on repos from hop.yaml.\n\nGetting started:\n  1. Run `hop config init` to create a starter hop.yaml.\n  2. Edit it to list your repos (each entry: name + git URL + parent dir).\n  3. Optional: set $HOP_CONFIG in your shell rc to point at a tracked file\n     (git-tracked dotfile, Dropbox, etc.) so it follows you across machines.\n  4. For interactive use, install the shim: eval \"$(hop shell-init zsh)\"\n\nUsage:\n  hop                       fzf picker, print selection\n  hop <name>                cd into the repo (shell function — needs `eval \"$(hop shell-init zsh)\"`)\n  hop <name>/<wt>           same, but rooted at <wt> (a worktree of <name> per `wt list --json`)\n  hop <name> cd             same — explicit verb form\n  hop <name> where          echo abs path of matching repo (or worktree, with /<wt> suffix)\n  hop <name> open           open the repo in an app (delegates to wt's menu; \"Open here\" cds the parent shell)\n  hop <name> -R <cmd>...    shim-only — run <cmd>... with cwd = <name>'s repo dir (or worktree)\n  hop -R <name> <cmd>...    binary-direct exec form (also reached via the shim)\n  hop <name> <tool>...      shim-only sugar for `hop -R <name> <tool> ...` (e.g., `hop dotfiles cursor`)\n  hop clone <name>          git clone the repo if it isn't already on disk\n  hop clone <url>           ad-hoc clone: clone the URL, register it in hop.yaml, print landed path\n  hop clone --all           clone every repo from hop.yaml that isn't already on disk\n  hop clone                 fzf picker, then clone if missing\n  hop pull <name>           Run 'git pull' in the named repo\n  hop pull <group>          Run 'git pull' in every cloned repo of <group>\n  hop pull --all            Run 'git pull' in every cloned repo\n  hop push <name>           Run 'git push' in the named repo\n  hop push <group>          Run 'git push' in every cloned repo of <group>\n  hop push --all            Run 'git push' in every cloned repo\n  hop sync <name>           Run 'git pull --rebase' then 'git push' in <name>\n  hop sync <group>          Run sync in every cloned repo of <group>\n  hop sync --all            Run sync in every cloned repo\n  hop ls                    list all repos\n  hop ls --trees            list all repos with worktree summaries (fans out `wt list --json`)\n  hop shell-init <shell>    emit shell integration (zsh or bash). Use: eval \"$(hop shell-init zsh)\"\n  hop config init           bootstrap a starter hop.yaml\n  hop config where          print the resolved hop.yaml path\n  hop config print          print the resolved hop.yaml contents to stdout\n  hop config scan <dir>     scan a directory for git repos and populate hop.yaml\n  hop update                self-update the hop binary via Homebrew\n  hop -h | --help           show this help\n  hop -v | --version        print version\n\nNotes:\n  - `hop <name>` and `hop <name> cd` require shell integration (a binary can't change\n    its parent shell's cwd). Without it, use:  cd \"$(hop <name> where)\"\n  - `hop <name> open`'s \"Open here\" choice also requires the shell shim to cd. Other\n    menu choices (editors, terminals, file managers) launch their app directly.\n  - The repo-first `hop <name> -R <cmd>...` and `hop <name> <tool>...` forms are shim-only\n    rewrites — the binary's `-R` parser expects `hop -R <name> <cmd>...`. Scripts and CI\n    that bypass the shim must use the binary-direct form `hop -R <name> <cmd>...` (and\n    `hop <name> where` for path resolution, which the binary handles directly).\n  - `pull`, `push`, and `sync` accept a repo name OR a group name (exact match) as the\n    positional, plus `--all` for the full registry. `sync` is `pull --rebase` + `push` —\n    linear history, no auto-resolve on conflict.\n  - On ambiguous or no-match queries, fzf opens prefilled with your query.\n  - Grammar: first positional is a repo OR a subcommand (mutually exclusive). When it's\n    a repo, second positional is a verb (`cd`, `where`, `open`), `-R`, or a tool name.\n  - Config search order: $HOP_CONFIG, then $XDG_CONFIG_HOME/hop/hop.yaml, then $HOME/.config/hop/hop.yaml.\n\nUsage:\n  hop\n  hop [command]\n\nAvailable Commands:\n  clone       git clone the resolved repo, an ad-hoc URL, or all missing repos with --all\n  completion  Generate the autocompletion script for the specified shell\n  config      config helpers (init, where, scan, add, rm, print)\n  help        Help about any command\n  ls          list all repos as aligned name/path columns\n  pull        Run 'git pull' in a repo, group, or every cloned repo with --all\n  push        Run 'git push' in a repo, group, or every cloned repo with --all\n  shell-init  emit shell integration (zsh or bash)\n  sync        Auto-commit dirty trees, then 'git pull --rebase' and 'git push' in a repo, group, or every cloned repo with --all\n  update      self-update the hop binary via Homebrew\n\nUse \"hop [command] --help\" for more information about a command.\n",
    "commands": [
      {
        "name": "clone",
        "path": "hop clone",
        "short": "git clone the resolved repo, an ad-hoc URL, or all missing repos with --all",
        "usage": "hop clone [<name> | <url> | --all] [flags]",
        "text": "Usage:\n  hop clone [<name> | <url> | --all] [flags]\n\nFlags:\n      --all            clone every repo from hop.yaml that isn't already on disk\n      --group string   target group for ad-hoc URL clone (only used with <url>) (default \"default\")\n      --name string    override the URL-derived name for the on-disk path (only used with <url>)\n      --no-add         skip the hop.yaml write-back (only used with <url>)\n      --no-cd          suppress the printed path so the shell shim does not cd (only used with <url>)\n",
        "commands": []
      },
      {
        "name": "config",
        "path": "hop config",
        "short": "config helpers (init, where, scan, add, rm, print)",
        "usage": "hop config",
        "text": "Usage:\n  hop config [command]\n\nAvailable Commands:\n  add         register a single on-disk repo into hop.yaml\n  init        bootstrap a starter hop.yaml at the resolved write target\n  print       print the resolved hop.yaml contents to stdout\n  rm          remove a registered repo from hop.yaml via an interactive picker\n  scan        scan a directory for git repos and populate hop.yaml\n  where       print the resolved hop.yaml path (regardless of file existence)\n\nUse \"hop config [command] --help\" for more information about a command.\n",
        "commands": [
          {
            "name": "add",
            "path": "hop config add",
            "short": "register a single on-disk repo into hop.yaml",
            "usage": "hop config add <dir>",
            "text": "Register a single on-disk repo into hop.yaml.\n\nThe non-recursive, single-directory sibling of 'hop config scan': it classifies\njust <dir> and, when it is a normal git repo with a remote, merges its URL into\nhop.yaml using the same group convention scan uses (convention layout → the\n'default' group; otherwise an invented group keyed off the parent dir basename).\n\nUnlike scan, add writes by default — you named a specific directory.\n\nA non-git directory is a no-op (a clear message, exit 0), not an error.\n\nExamples:\n  hop config add ~/code/acme/widget   register one existing repo into hop.yaml\n\nUsage:\n  hop config add <dir>\n",
            "commands": []
          },
          {
            "name": "init",
            "path": "hop config init",
            "short": "bootstrap a starter hop.yaml at the resolved write target",
            "usage": "hop config init",
            "text": "Usage:\n  hop config init\n",
            "commands": []
          },
          {
            "name": "print",
            "path": "hop config print",
            "short": "print the resolved hop.yaml contents to stdout",
            "usage": "hop config print",
            "text": "Usage:\n  hop config print\n",
            "commands": []
          },
          {
            "name": "rm",
            "path": "hop config rm",
            "short": "remove a registered repo from hop.yaml via an interactive picker",
            "usage": "hop config rm [--stale] [flags]",
            "text": "Remove a registered repo from hop.yaml via an interactive picker.\n\nPipes the registered repos through fzf; the selected entry's URL is removed\nfrom its group. Removing a group's last URL leaves the (now-empty) group as a\nplaceholder, so it stays a valid 'hop clone --group' target.\n\nWith --stale, the picker is pre-filtered to repos whose resolved path no longer\nexists on disk — the quick way to prune entries for repos you have deleted.\n\nExamples:\n  hop config rm           pick any registered repo to remove\n  hop config rm --stale   pick among only the repos missing from disk\n\nUsage:\n  hop config rm [--stale] [flags]\n\nFlags:\n      --stale   limit the picker to repos whose resolved path no longer exists on disk\n",
            "commands": []
          },
          {
            "name": "scan",
            "path": "hop config scan",
            "short": "scan a directory for git repos and populate hop.yaml",
            "usage": "hop config scan <dir> [flags]",
            "text": "Scan a directory for git repos and populate hop.yaml.\n\nAuto-derives groups from the on-disk layout: repos at <code_root>/<org>/<name>\nland in the 'default' flat group; non-convention repos land in invented\nmap-shaped groups keyed off the parent dir basename.\n\nExamples:\n  hop config scan ~/code              print the rendered YAML to stdout\n  hop config scan ~/code --write      merge into the resolved hop.yaml in place\n  hop config scan ~/code --depth 4    extend the walk one level deeper\n\nUsage:\n  hop config scan <dir> [flags]\n\nFlags:\n      --depth int   maximum DFS depth (root counts as depth 0; must be >= 1) (default 3)\n      --write       merge results into the resolved hop.yaml (atomic, comment-preserving). Default: render to stdout.\n",
            "commands": []
          },
          {
            "name": "where",
            "path": "hop config where",
            "short": "print the resolved hop.yaml path (regardless of file existence)",
            "usage": "hop config where",
            "text": "Usage:\n  hop config where\n",
            "commands": []
          }
        ]
      },
      {
        "name": "ls",
        "path": "hop ls",
        "short": "list all repos as aligned name/path columns",
        "usage": "hop ls [flags]",
        "text": "Usage:\n  hop ls [flags]\n\nFlags:\n      --trees wt list --json   list worktrees per repo via wt list --json\n",
        "commands": []
      },
      {
        "name": "pull",
        "path": "hop pull",
        "short": "Run 'git pull' in a repo, group, or every cloned repo with --all",
        "usage": "hop pull [<name-or-group>] [--all] [flags]",
        "text": "Usage:\n  hop pull [<name-or-group>] [--all] [flags]\n\nFlags:\n      --all   run 'git pull' in every cloned repo from hop.yaml\n",
        "commands": []
      },
      {
        "name": "push",
        "path": "hop push",
        "short": "Run 'git push' in a repo, group, or every cloned repo with --all",
        "usage": "hop push [<name-or-group>] [--all] [flags]",
        "text": "Usage:\n  hop push [<name-or-group>] [--all] [flags]\n\nFlags:\n      --all   run 'git push' in every cloned repo from hop.yaml\n",
        "commands": []
      },
      {
        "name": "shell-init",
        "path": "hop shell-init",
        "short": "emit shell integration (zsh or bash)",
        "usage": "hop shell-init <shell>",
        "text": "Usage:\n  hop shell-init <shell>\n",
        "commands": []
      },
      {
        "name": "sync",
        "path": "hop sync",
        "short": "Auto-commit dirty trees, then 'git pull --rebase' and 'git push' in a repo, group, or every cloned repo with --all",
        "usage": "hop sync [<name-or-group>] [--all] [-m <msg>] [flags]",
        "text": "Usage:\n  hop sync [<name-or-group>] [--all] [-m <msg>] [flags]\n\nFlags:\n      --all              run sync (pull --rebase + push) in every cloned repo from hop.yaml\n  -m, --message string   commit message used when auto-committing a dirty tree (no effect on clean trees) (default \"chore: sync via hop\")\n",
        "commands": []
      },
      {
        "name": "update",
        "path": "hop update",
        "short": "self-update the hop binary via Homebrew",
        "usage": "hop update [flags]",
        "text": "Usage:\n  hop update [flags]\n\nFlags:\n      --skip-brew-update brew update   skip the internal brew update tap-metadata refresh (the version check and upgrade still run)\n",
        "commands": []
      }
    ]
  }
}
