# Plan: Invert help-collection transport from PUSH to PULL

**Change**: 260603-oa63-invert-help-transport-pull
**Status**: In Progress
**Intake**: `intake.md`
**Spec**: `spec.md`

<!--
  AUTO-GENERATED by /fab-continue at the apply stage entry. Apply parses ## Tasks
  only; review parses ## Acceptance only. This is a docs/CI refactor (transport
  inversion). No source code under sites/.../src is touched — the consume side is
  provably untouched (spec §Consume side untouched).
-->

## Tasks

### Phase 1: Setup

<!-- No scaffolding/deps. The first content artifact (the forward contract spec)
     is the keystone everything else cross-links to, so it is built first. -->

- [x] T001 Create the forward-contract spec `docs/specs/help-dump-contract.md` with Overview (tool's single obligation + anti-drift framing), §1 Invocation, §2 Hidden/self-filtering, §3 Output schema (envelope + recursive Node verbatim, the `captured_at` asymmetry), §4 Filter rules, §5 Discovery & recursion, §6 Version from binary, §7 `tu` exception, §8 Schema evolution, a bounded DATED (2026-06-03) §Pull model migration section, a §Schema reference cross-link to `sites/astro-starlight-terminal1/src/lib/schemas.ts`, and a Changelog entry dated 2026-06-03 referencing oa63. <!-- A-001 A-002 A-003 A-004 A-005 A-006 A-007 A-008 A-009 A-010 A-011 -->

### Phase 2: Core Implementation

- [x] T002 Add a row for the new spec to the `docs/specs/index.md` table (currently empty). <!-- A-012 -->
- [x] T003 Create the puller workflow `.github/workflows/scheduled-help-refresh.yml`: daily `schedule` cron + `workflow_dispatch`; per-tool brew install (`brew install sahil87/tap/<tool>`) → `<tool> help-dump` → stamp `captured_at` → stage to `help/<slug>.json`; reuse deploy.yml's pnpm 10 + Node 22 + frozen-lockfile pattern (cache keyed on the live site lockfile) to run the EXISTING `node scripts/validate-help.mjs`; per-tool failure isolation (never clobber last-good; missing tu.json tolerated); direct commit to `main` GATED on validation passing; minimal `contents: write` permissions; OFF the deploy path (comment noting deploy.yml push-to-main ships it, Constitution IV). <!-- A-013 A-014 A-015 A-016 A-017 A-018 A-019 A-020 -->
- [x] T004 Delete `.github/workflows/help-automerge.yml` in full (the push-receive transport incl. the three guards + SHLLAI_TOKEN trust model). <!-- A-021 -->

### Phase 3: Integration & Edge Cases (memory rewrites + cross-links)

- [x] T005 Rewrite `docs/memory/conventions/help-collection.md` DOWN to consume/pull: remove the push-model docs (Producer Rules push portion, Push & Receive Flow, the three guards, push-related Design Decisions); slim the schema prose to a pointer to the new spec; ADD the PULL mechanism (scheduled refresh: brew install → help-dump → stamp captured_at → validate → direct-commit-gated, off the deploy path); cross-link `docs/specs/help-dump-contract.md` as the forward contract; KEEP the Rendering Consumer / CommandReference section, the `help/` root collector placement + cross-boundary read, and the `schemas.ts` anchor; add a changelog entry for oa63 (2026-06-03). <!-- A-022 A-023 A-024 A-025 A-026 -->
- [x] T006 [P] Update the `help-collection` one-line description in `docs/memory/conventions/index.md` (remove "PR + auto-merge flow"; describe the pull/consume model); refresh its Last Updated date. <!-- A-027 -->
- [x] T007 [P] Replace the "## Inbound help-collection PRs" section in `docs/memory/build-deploy/deployment.md` with the pull model (scheduled refresh off the deploy path; existing push-to-`main` deploy ships the committed result, Constitution IV); add a changelog entry for oa63 (2026-06-03). <!-- A-028 A-029 -->
- [x] T009 [P] Drive-by config fix (user-approved): update `fab/project/config.yaml` `source_paths` from the stale `sites/astro-tailwind-terminal1/src/` to the live `sites/astro-starlight-terminal1/src/` (matches `SITE_DIR` in deploy.yml). <!-- A-033 -->

### Phase 4: Polish (validation)

- [x] T008 Validate: run the existing `node scripts/validate-help.mjs` from the site dir to prove the consume side + committed `help/*.json` still validate (none changed); parse the new workflow YAML for syntactic validity; confirm `help-automerge.yml` is gone and no consume-side files (CommandReference.astro, schemas.ts, validate-help.mjs, astro-content-alias.mjs, deploy.yml) were touched. <!-- A-030 A-031 A-032 -->

## Execution Order

- T001 blocks T002, T005, T007 (they cross-link the spec by path) — write the spec first so the link target exists.
- T003 and T004 are independent of the docs work and of each other; T004 (delete) can run anytime.
- T006 and T007 are independent docs edits ([P]) but conceptually follow T005's rewrite (consistent wording).
- T008 runs last (validates the whole set).

## Acceptance

### Functional Completeness

- [x] A-001 Contract spec exists & indexed: `docs/specs/help-dump-contract.md` exists with Overview, §1–§8, a bounded dated migration section, and a Schema-reference cross-link.
- [x] A-002 §Overview states the tool's ONLY obligation ("emit valid `help-dump` output; everything downstream is shll.ai's job") and carries the anti-drift framing (help is machine-generated, never hand-maintained).
- [x] A-003 §1 Invocation: uniform `<tool> help-dump`; JSON envelope to STDOUT; stderr empty on success; exit 0 on success; non-zero = hard capture failure for the puller.
- [x] A-004 §2: `help-dump` MUST be Cobra `Hidden: true` and filter ITSELF out of the emitted tree, covered by the §4 Hidden-node filter (no special case).
- [x] A-005 §3 reproduces the envelope + recursive Node verbatim (self-contained) and states the `captured_at` asymmetry: TOOL-EMITTED = `{tool, version, schema_version, root}` (no captured_at); VALIDATED POST-CAPTURE adds puller-stamped `captured_at`. Node = `{name, path, short, usage, text, commands: Node[]}`, recursive, leaf `commands:[]`, carries raw text AND structured short/usage/path.
- [x] A-006 §4 Filter rules: drop Cobra `completion` + `help` auto-subcommands and any `Hidden` node (now including help-dump itself).
- [x] A-007 §5: walk `rootCmd.Commands()` programmatically, full recursion to any depth; NEVER regex-parse `-h`.
- [x] A-008 §6: `version` from the built binary (ldflags / `rootCmd.Version`), never hardcoded; the wt.json `1.4.2` placeholder caveat is noted.
- [x] A-009 §7 `tu` exception: Node/TS, flat tree (`root.commands:[]`), full `tu --help` in `root.text`; tu's help-dump IN PROGRESS so `help/tu.json` MAY be MISSING (expected interim, consumer degrades MISSING→placeholder).
- [x] A-010 §8 Schema evolution: `schema_version` integer, current = 1; new fields added OPTIONAL for tool-by-tool rollout.
- [x] A-011 §Schema reference points to `sites/astro-starlight-terminal1/src/lib/schemas.ts` as the single machine-checkable anchor; states prose↔code must agree; cross-links both ways; a changelog entry dated 2026-06-03 references oa63.
- [x] A-012 `docs/specs/index.md` lists a row pointing at `help-dump-contract.md`.
- [x] A-013 `.github/workflows/scheduled-help-refresh.yml` exists with triggers `schedule` (a DAILY cron) + `workflow_dispatch`, and NO push/deploy trigger. Runs on `ubuntu-latest` with a "Set up Homebrew" step that loads `brew shellenv` into `$GITHUB_PATH`/`$GITHUB_ENV` (Linuxbrew is preinstalled but off-PATH).
- [x] A-014 The workflow installs each of the 7 tools via `brew install sahil87/tap/<tool>`, runs `<tool> help-dump` capturing STDOUT to `help/<slug>.json` (slug-named file; binary carried in `tool` field — slug→binary mapping correct: idea→idea, hop→hop, fab-kit→fab, wt→wt, run-kit→rk, tu→tu, shll→shll).
- [x] A-015 The workflow stamps `captured_at` (ISO-8601 UTC) into each captured file (shll.ai-owned field, §3 asymmetry).
- [x] A-016 The workflow reuses deploy.yml's pnpm 10 + Node 22 + `pnpm install --frozen-lockfile` pattern (cache keyed on the live site lockfile) and runs the EXISTING `node scripts/validate-help.mjs` from the site dir.
- [x] A-017 The commit is a DIRECT COMMIT to `main`, GATED on `validate-help.mjs` passing (only validated files reach main); no PR, no auto-merge, no `SHLLAI_TOKEN`; uses default `GITHUB_TOKEN` with a minimal `contents: write` permissions block; commits only on actual change.
- [x] A-018 `.github/workflows/help-automerge.yml` no longer exists; `scheduled-help-refresh.yml` is its replacement inbound path.
- [x] A-019 `docs/memory/conventions/help-collection.md` no longer documents PR + auto-merge / three guards / `SHLLAI_TOKEN`; describes the new pull mechanism off the deploy path; cross-links the contract spec; retains the Rendering Consumer section, the `help/` root collector rationale + cross-boundary read, and the `schemas.ts` anchor; has an oa63 changelog row.
- [x] A-020 `docs/memory/conventions/index.md` `help-collection` description no longer says "PR + auto-merge" and reflects the pull/consume model.
- [x] A-021 `docs/memory/build-deploy/deployment.md` "Inbound help-collection PRs" section is replaced with the scheduled refresh pull model off the deploy path (existing push-to-`main` deploy ships the committed result, Constitution IV), with an oa63 changelog row.

### Behavioral Correctness

- [x] A-022 Per-tool CAPTURE-failure isolation: a failed `brew install`, non-zero `help-dump` exit, empty output, or non-JSON output for one tool does NOT clobber that tool's last-good committed `help/<slug>.json` and does NOT block the other tools (the capture loop stages per-tool into a temp file and only overwrites on a successful capture). Schema validity is enforced COLLECTIVELY by the whole-directory `validate-help.mjs` gate: a valid-JSON-but-schema-invalid capture gates off the whole commit that run (every tool keeps last-good, run self-heals) — an accepted freshness-over-complexity tradeoff with safety preserved (spec §Per-tool capture-failure isolation).
- [x] A-023 Off the deploy path: `deploy.yml` is byte-identical to its pre-change state; the refresh job adds no new deploy trigger; committing to `main` relies on the existing `deploy.yml` push-to-`main` deploy. A comment in the workflow records this.
- [x] A-024 Schema FROZEN: `schema_version` stays `1`; no new fields introduced; the contract's tool-emitted `wt` shape `{tool, version, schema_version, root}` is equivalent to committed `help/wt.json` modulo the puller-stamped `captured_at`.

### Removal Verification

- [x] A-025 The push-model documentation is gone everywhere it was removed: help-automerge.yml (deleted), the help-collection.md push subsections + three guards + push Design Decisions, and the deployment.md inbound-PR three-guards description.

### Scenario Coverage

- [x] A-026 Missing `help/tu.json` is treated as the expected interim state (documented in the contract §7 and reflected in the workflow's per-tool isolation), not a hard failure.
- [x] A-027 Sequencing risk recorded: the contract's §Pull model migration section states teardown is only safe once the puller is live and proven (the four deletable items listed: producer CI, PR-opening, auto-merge, `SHLLAI_TOKEN`), and notes the section collapses into the changelog after all 7 repos finish; the 7-repo teardown is out of scope.

### Edge Cases & Error Handling

- [x] A-028 Malformed capture is blocked: a captured file failing `validate-help.mjs` gates off the commit so it never reaches `main`.
- [x] A-029 A broken tool release / unavailable brew tap breaks only the REFRESH, not `deploy.yml`; the site keeps shipping last-good committed help.

### Code Quality

- [x] A-030 Pattern consistency: the new spec/workflow/memory follow existing project doc + workflow conventions (tone, headings, changelog table format, YAML setup pattern from deploy.yml/help-automerge.yml).
- [x] A-031 No unnecessary duplication: the schema is defined in exactly TWO places — the contract spec (prose) and `schemas.ts` (code) — not duplicated in `help-collection.md` (which points to the spec); no magic literals beyond the documented placeholders.
- [x] A-032 Consume side provably untouched: none of `CommandReference.astro`, `src/lib/schemas.ts`, `scripts/validate-help.mjs`, `scripts/astro-content-alias.mjs`, or `deploy.yml` appear in the change diff; the existing `help/*.json` still validate.

### Drive-by Config Fix

- [x] A-033 `fab/project/config.yaml` `source_paths` points at the live site `sites/astro-starlight-terminal1/src/` (not the stale `astro-tailwind-terminal1`), matching `SITE_DIR` in `deploy.yml`.

## Notes

- Check items as you review: `- [x]`
- Slug→binary mapping VERIFIED against `help/*.json` `tool` fields: fab-kit→fab, run-kit→rk, hop→hop, idea→idea, shll→shll, wt→wt; tu→tu (file absent, expected).
- Brew-runner decision: resolved by the user to `ubuntu-latest` + Linuxbrew (the tap is confirmed Linux-capable). Recorded in the workflow header + the "Set up Homebrew" step.
  <!-- clarified: brew runner = ubuntu-latest + Linuxbrew. User confirmed sahil87/tap ships Linux-capable formulae for all 7 CLIs, so the cheaper/faster Linux runner is used over macos-latest (~10x CI minutes). The "Set up Homebrew" step loads `brew shellenv` into $GITHUB_PATH/$GITHUB_ENV (Linuxbrew preinstalled on ubuntu-latest but off-PATH). -->

## Clarifications

### Session 2026-06-03 (auto mode, then user)

| # | Item | Resolution |
|---|------|------------|
| 1 | Brew runner choice (`runs-on`) for `scheduled-help-refresh.yml` | Auto-mode graded NON-BLOCKING (safe default, high reversibility). Then surfaced to the user, who chose **`ubuntu-latest` + Linuxbrew** — the tap is confirmed Linux-capable, so the cheaper/faster runner wins. Workflow updated: `runs-on: ubuntu-latest` + a "Set up Homebrew" step loading `brew shellenv`. |
| 2 | Stale `source_paths` in `fab/project/config.yaml` (flagged by spec + clarify sub-agents) | User chose to **fix it in this change** (drive-by): `sites/astro-tailwind-terminal1/src/` → `sites/astro-starlight-terminal1/src/`. Added as task T009 / acceptance A-033. |
