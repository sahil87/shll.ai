---
description: "Recurring shapes and patterns that span multiple files"
---
# conventions

> **Generated by `fab memory-index`** — do not hand-edit. Descriptions come from each file's `description:` frontmatter.

| File | Description |
|------|-------------|
| [docs-site-tree](docs-site-tree.md) | The `docs/site/**` documentation-tree consume/pull side (sibling of readme-extraction; contract §9 now active — change `x0br`): the closed-set producer contract, **site-absolute** link resolution (`rewriteDocsSiteLinks`/`rewriteReadmeDocsSiteLinks`, trailing-slash-directory-proof), the single-scanner rewrite guard, the report-only closure + reserved-slug lints (`findClosureViolations`, `extract-docs-site-cli.mjs`, never withhold), the repo-tarball pull step in `refresh-readme.yml`, the first dynamic route (`[slug]/[...path].astro` — mounted at the ROOT namespace `/<slug>/<path>` since change `3ke3`, formerly `tools/[slug]/…`; build-time `getStaticPaths` → `<StarlightPage>`), the reverse-redirect enumeration for old `/tools/<slug>/<path>` deep URLs (`docsSiteRedirectEntries`, change `3ke3`), and the `docs-site-tree.ts` / `.mjs` sidebar split (config-eval boundary) |
| [help-collection](help-collection.md) | The `help/<tool>.json` consume/pull side: Zod anchor, `help/` collector, the rendering consumers (build-time `parseHelp` structured render + cross-tool index; the homepage terminal's tool cards — change `37ng` — slim embedded payload, per-tool skip-degrade vs. VersionTable's build-stop; and — change `354p` — the `/llms.txt`/`/llms-full.txt` agent-discoverability endpoints reading `root.short` for the index and the command tree for the full dump), the scheduled brew + `help-dump` pull refresh (forward contract in `docs/specs/help-dump-contract.md`), the command/flag cross-check's two modes (hard rule for hand-written prose, report-only lint for pulled README prose), and the sibling README-pull consumer |
| [readme-extraction](readme-extraction.md) | The README consume/pull side (sibling of help-collection): deduction rules (head/tail/strips) in `extract-readme.ts`, the `content/<slug>/` collector, `ReadmeSlice.astro` rendered on a parallel per-tool `readme` page, the `readme-toc.ts` rail builder (second slice consumer, slugs single-sourced through the one `createMarkdownProcessor` render — `id === href`, no `github-slugger` dep), the `/llms-full.txt` agent-discoverability dump (third slice consumer — change `354p`, reads the slice verbatim so `<TAB>`/`<N>` placeholders survive, unlike the flattened MDX), the `refresh-readme.yml` always-commit daily pull, the report-only `vn39` divergence reporter + the CLI's four exit behaviors, and the `@astrojs/markdown-remark` reconciliation (forward contract in `docs/specs/readme-extraction-contract.md`) |
| [seo-social-meta](seo-social-meta.md) | The SEO & social-share layer on the live Starlight site PLUS its agent-discoverability sibling AND the editorial/content SEO layer: Starlight's emitted-by-default baseline (do not re-add it), the site-wide og:image/twitter:image set appended in the `Head.astro` override (absolute URL from `Astro.site`), the committed 1200×630 terminal-card `og-image.png` + its unwired one-off generator script, the homepage frontmatter `head:` overrides (keyword title, `og:type=website`), the JSON-LD route dispatcher in `Head.astro` — homepage WebSite+SoftwareApplication graph (`kb1r`) plus a per-tool SoftwareApplication+BreadcrumbList graph on the roster-gated per-tool ROOT pages (change `pgox`; namespace moved to root by change `3ke3` — the overview `/<tool>/` 1-segment + readme/commands `/<tool>/<page>/` 2-segment, gated on the shared tool-slug roster; description single-sourced from `help/<tool>.json` `root.short`, absolute URLs from `Astro.site`, missing→omit-description / invalid→build-fail), the redirect-stub exclusion (now the old `/tools/<tool>/*` deep URLs, reversed by change `3ke3`), (change `354p`) the build-time `/llms.txt` + `/llms-full.txt` static endpoints — the agent-facing sibling of the crawler/scraper SEO layer (llmstxt.org index + full-content dump, single-sourced from `help/<tool>.json` `root.short` and the README/MDX the HTML pages render, absolute URLs from `Astro.site`), and (change `bees`) the editorial/content layer: meta descriptions are HAND-AUTHORED + SEO-optimized (NOT `root.short`-sourced — the inverse of pgox/354p), plus the cross-tool internal-linking convention (the `idea→fab-kit→wt→run-kit` graph) realized as a `## How it fits` subsection on every overview |
| [tool-page-rubric](tool-page-rubric.md) | The shape of every tool page, incl. the current site-authored overview body shape (change `bees` — `<GithubButton>` + a 3–4-sentence job-framed lead + a `## How it fits` cross-tool linking subsection wiring the `idea→fab-kit→wt→run-kit` graph + nav links; the `vn39` HARD rule now ACTIVELY binds this framing prose, which names validated CLI tokens like `run-kit riff`/`shll install`, while slash-command skills/file-paths/shell-builtins stay exempt; the `jf3q` `rk` vs `run-kit` naming rule INVERTED at `co63` — run-kit v3.0.0 renamed `rk` → `run-kit` as canonical CLI/formula/binary, so slug=binary=`run-kit` and `rk` is only the terminal's short-alias affordance, leaving fab-kit the lone slug≠binary divergence), the per-tool GitHub affordance (now carrying a fail-soft build-time star count — `d9qb`), `.md`→`.mdx` overviews, the enriched generated command reference + cross-tool index, the pulled-README-slice exception (dedicated per-tool `readme` page, sibling of `commands`, thinned overview, report-only divergence — constitution v2.1.1 Tool-Page Depth), the generated right-rail "On this page" ToC + the single-override-slot dispatcher pattern, the site chrome overrides (run-kit hexagon header logo, the explicit `favicon: ''/favicon.svg''` + the real multi-resolution `.ico` fallback fixing headless-route tab icons — `mb9s`, Footer/Head component overrides — the Footer also carrying the `d9qb` author-link row, the Head override the `kb1r` SEO/social-share layer + the `pgox` per-tool JSON-LD dispatcher, see seo-social-meta), the `/tools` index directory page (`pgox` — `ToolsIndex.astro`, build-time JSON-single-sourced one-liners as a NON-drift surface, `VersionTable` build-stop posture, unlinked sidebar group, `/tools/` still the directory landing after change `3ke3`); the per-tool pages promoted to canonical ROOT slugs (change `3ke3` — `/<tool>` overview + `/<tool>/readme` + `/<tool>/commands` via Starlight `slug:` overrides, old `/tools/<tool>/*` reversed into redirects, shared `tool-slugs.ts` roster gates the dispatchers, plus the root-namespace reservation for future tool slugs — see the "adding an 8th tool" note); and the hand-authored homepage newcomer blocks (`ld0j` — seven overview links + per-tool one-liners + a second install-commands hand-copy, both recorded as drift surfaces; plus the `d9qb` `$ whoami` author block, whose GitHub/LinkedIn/noon.design links are a THREE-WAY footer/homepage/terminal-egg hand-copy — LinkedIn handle `ahujasahil`, not `sahil87`) |
