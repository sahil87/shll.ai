# Log seed — build-deploy
<!-- Pre-FKF changelog entries, merged beneath the git projection by `fab memory-index`. -->

## 2026-06-13
- **Update** [deployment](/build-deploy/deployment.md) — Change `xs1j`: **fixed the broken refresh→deploy cascade and renamed the two refresh workflows.** Corrected this file's false "the help refresh is the reliable daily deploy **driver**" claim: a push made with the default `GITHUB_TOKEN` is suppressed from triggering `deploy.yml`'s `on: push` by GitHub's recursion guard, so before this change the help refresh committed daily but did **not** deploy — only a human merge deployed. Each refresh job now explicitly dispatches `deploy.yml` via `workflow_dispatch` (`gh workflow run deploy.yml --ref main`) after — and only after — it pushes a real commit (the no-op `exit 0` path dispatches nothing); `actions: write` added to each job's `permissions:` and `GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}` in the dispatch step env — **no new secret** (`workflow_dispatch` is exempt from the recursion guard). The help refresh's `captured_at` churn is now the reliable daily *commit* AND (via the dispatch) *deploy* driver; the README refresh dispatches only when a slice changed. Renamed `scheduled-help-refresh.yml` → `refresh-help.yml` (`'Refresh: Help'`, group `refresh-help`) and `scheduled-readme-refresh.yml` → `refresh-readme.yml` (`'Refresh: README'`, group `refresh-readme`); `deploy.yml` `name:` `Deploy to GitHub Pages` → `Deploy` (no trigger change — it already declared `workflow_dispatch`). No cron change (07:13 / 07:41 offset preserved); no schedule added to `deploy.yml`. (xs1j)

## 2026-06-11
- **Update** [deployment](/build-deploy/deployment.md) — Change `d9qb`: `deploy.yml`'s "Build with Astro" step now passes the automatic `GITHUB_TOKEN` as **step-scoped env** (rate-limit headroom for the new build-time star-count fetch; `permissions` block unchanged — `contents: read` suffices). Added the [Build-time GitHub API dependency](#build-time-github-api-dependency-star-counts-change-d9qb) section: the build makes 7 tolerant `api.github.com/repos/sahil87/<tool>` calls (`src/lib/github-stars.ts`), **fail-soft to count-omission** — any failure (network, non-200, malformed body, 10s timeout) → `null` + one warning, NEVER a failed build (the deliberate opposite of `VersionTable`'s build-stop). Added the [corrected daily-deploy freshness model](#daily-deploy-freshness-model-corrected-change-d9qb), validated during review: **neither** scheduled pull literally always-commits (both skip on `git diff --cached --quiet`); the reliable daily-deploy driver is the **help refresh's per-run `captured_at` churn**, while the README refresh commits only when slice content changed — corrected this file's prior "always direct-commits" phrasing to the precise ungated-by-divergence meaning. (d9qb)

## 2026-06-08
- **Update** [deployment](/build-deploy/deployment.md) — Change `i2b0`: added the [Analytics](#analytics-cloudflare-web-analytics-change-i2b0) section — the live site now carries the **Cloudflare Web Analytics** beacon site-wide (cookieless → no consent gate; public token, hardcoded). Injected via a `Head.astro` component override emitting a literal `set:html` tag (Starlight's `head:` array HTML-escapes the `data-cf-beacon` JSON). A **deliberate, user-approved Constitution I exception** (client-side runtime JS, scoped to analytics only, `defer`red, no new dependency — Constitution VI respected). Off the deploy plumbing; ships via the normal push-to-`main` deploy. (i2b0)

## 2026-06-04
- **Update** [deployment](/build-deploy/deployment.md) — Change `4s3e`: the README-refresh pull now **always commits** the slice — its commit is no longer gated on the `vn39` cross-check (the tool README is canonical and committed verbatim; divergence emits a non-fatal CI `::warning::` instead of skipping the tool). Per-tool "keep last-good" isolation now applies ONLY to genuine **fetch failures**, not to divergence. The help-refresh path is unchanged (its commit stays gated on Zod `validate-help.mjs`), creating a deliberate gate asymmetry between the two inbound pulls. See [readme-extraction](/conventions/readme-extraction.md). (4s3e)
- **Update** [deployment](/build-deploy/deployment.md) — Change `w32m`: added a **second inbound scheduled pull path** — `.github/workflows/scheduled-readme-refresh.yml` (daily cron + dispatch, a sibling of the help-refresh) fetches each tool's `README.md`, deduces the curated slice, runs the `vn39` command/flag gate, and direct-commits `content/<slug>/README.md` to `main` gated on the gate passing, per-tool isolated and OFF the deploy path. Generalized the section to "Inbound scheduled pulls" (two siblings: help → `help/*.json`, README → `content/<slug>/README.md`). The existing push-to-`main` deploy still ships both committed results (Constitution IV); `deploy.yml` unchanged. See [readme-extraction](/conventions/readme-extraction.md). (w32m)

## 2026-06-03
- **Update** [deployment](/build-deploy/deployment.md) — Change `oa63`: inverted the inbound help path from PUSH to PULL. Replaced the "Inbound help-collection PRs" section (tool-repo PRs + `help-automerge.yml` + three guards + `SHLLAI_TOKEN`) with the scheduled pull model — `.github/workflows/scheduled-help-refresh.yml` (daily cron + dispatch) brew-installs the 7 tools, runs `help-dump`, stamps `captured_at`, validates, and direct-commits to `main` gated on validation, deliberately OFF the deploy path. The existing push-to-`main` deploy still ships the committed result (Constitution IV); `deploy.yml` is unchanged. (oa63)

## 2026-06-02
- **Update** [deployment](/build-deploy/deployment.md) — Change `xiis`: documented the inbound help-collection PR path (tool repos PR `help/<slug>.json` into shll.ai), the `help-automerge.yml` auto-merge receiving workflow scoped to `help/**`, and that the existing push-to-`main` deploy ships the result (no new deploy workflow). (xiis)
- **Update** [deployment](/build-deploy/deployment.md) — Change `xiis` (review rework): clarified that `help-automerge.yml`'s `help/**` path filter controls *triggering* not merge eligibility, and that auto-merge is gated by three guards (content / schema-validation / actor `sahil87`) — required because the repo is public and `main` is unprotected and auto-deploys. *(Superseded by `oa63` — push model retired.)* (xiis)

## 2026-05-17
- **Creation** [deployment](/build-deploy/deployment.md) — Generated from code analysis. (—)
